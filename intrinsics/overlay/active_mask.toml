# Reviewed cuda-oxide policy for the dynamic warp mask.

schema = 26
family = "active_mask"

[[intrinsic]]
id = "active_mask"
abi_id = "i0044"
operation_key = "warp.active_mask"
family = "active_mask"
source_record = "int_nvvm_activemask"
rust_module = "warp"
rust_name = "active_mask"
rust_arguments = []
rust_result = "u32"
safe = true
must_use = true
safe_allowlist_reason = "reading the current warp's active-lane mask has no memory, pointer, synchronization, or participation preconditions."
public_rust_path = "cuda_intrinsics::warp::active_mask"
compatibility_rust_paths = ["cuda_device::warp::active_mask"]
dialect_op_type = "ActiveMaskOp"
dialect_op_name = "nvvm.activemask"
dialect_operands = []
dialect_results = ["i32"]
llvm_symbol = "llvm.nvvm.activemask"
llvm_arguments = []
llvm_results = ["i32"]
pure = false
memory = "inaccessible_read_write"
convergent = true
execution_scope = "warp"
minimum_ptx = "6.2"
minimum_sm = "sm_30"
ptx_result = "u32"
targets = "all"
ptx_isa_version = "9.3"
ptx_isa_section = "9.7.14.12 Parallel Synchronization and Communication Instructions: activemask"
ptx_isa_url = "https://docs.nvidia.com/cuda/parallel-thread-execution/#parallel-synchronization-and-communication-instructions-activemask"
lowering = "generated_active_mask"
backend_lowerings = [
    { backend = "llvm_nvptx", mechanism = "typed_nvvm", evidence_profile = "rust-llvm-23.1.0-16696adc", minimum_ptx = "6.2", minimum_sm = "sm_30" },
    { backend = "lib_nvvm", mechanism = "inline_ptx", evidence_profile = "cuda-13.3-libnvvm-13.3.33", minimum_sm = "sm_75" },
]
active_mask = { observation = "executing_lanes_at_instruction", adapter = "direct_zero_operand_mask" }
expected_ptx = { mnemonic = "activemask", modifiers = ["b32"], operands = [{ kind = "register" }] }
summary = "Returns the lanes active at this instruction in the current warp."
