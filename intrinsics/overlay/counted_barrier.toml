# Reviewed counted CTA barrier instruction family.

schema = 61
family = "counted_barrier"

[[intrinsic]]
id = "barrier_cta_sync"
abi_id = "i0883"
operation_key = "synchronization.cta.barrier.count"
family = "counted_barrier"
source_record = "int_nvvm_barrier_cta_sync_count"
rust_module = "barrier"
rust_name = "barrier_cta_sync"
rust_arguments = ["u32", "u32"]
rust_result = "()"
safe = false
must_use = false
public_rust_path = "cuda_intrinsics::barrier::barrier_cta_sync"
compatibility_rust_paths = ["cuda_device::barrier::barrier_cta_sync"]
dialect_op_type = "BarrierCtaSyncCountOp"
dialect_op_name = "nvvm.barrier_cta_sync_count"
dialect_operands = ["i32", "i32"]
dialect_results = []
llvm_symbol = "llvm.nvvm.barrier.cta.sync.count"
llvm_arguments = ["i32", "i32"]
llvm_results = []
pure = false
memory = "read_write"
convergent = true
execution_scope = "cta"
minimum_ptx = "6.0"
minimum_sm = "sm_30"
ptx_result = "()"
targets = "all"
ptx_isa_version = "9.3"
ptx_isa_section = "9.7.14.1 Parallel Synchronization and Communication Instructions: bar, barrier"
ptx_isa_url = "https://docs.nvidia.com/cuda/parallel-thread-execution/#parallel-synchronization-and-communication-instructions-bar-barrier"
lowering = "generated_execution_control"
backend_lowerings = [
    { backend = "llvm_nvptx", mechanism = "typed_nvvm", evidence_profile = "rust-llvm-23.1.0-16696adc-execution-control", minimum_ptx = "6.0", minimum_sm = "sm_30" },
    { backend = "lib_nvvm", mechanism = "inline_ptx", evidence_profile = "cuda-13.3-libnvvm-13.3.73-execution-control", minimum_ptx = "6.0", minimum_sm = "sm_75" },
]
expected_ptx = { mnemonic = "barrier", modifiers = ["sync"], operands = [{ kind = "register_or_immediate" }, { kind = "register_or_immediate" }] }
summary = "Synchronizes the requested number of CTA threads at a numbered barrier."

[[intrinsic]]
id = "barrier_cta_sync_aligned"
abi_id = "i0884"
operation_key = "synchronization.cta.bar.aligned.count"
family = "counted_barrier"
source_record = "int_nvvm_barrier_cta_sync_aligned_count"
rust_module = "barrier"
rust_name = "barrier_cta_sync_aligned"
rust_arguments = ["u32", "u32"]
rust_result = "()"
safe = false
must_use = false
public_rust_path = "cuda_intrinsics::barrier::barrier_cta_sync_aligned"
compatibility_rust_paths = ["cuda_device::barrier::barrier_cta_sync_aligned"]
dialect_op_type = "BarrierCtaSyncAlignedCountOp"
dialect_op_name = "nvvm.barrier_cta_sync_aligned_count"
dialect_operands = ["i32", "i32"]
dialect_results = []
llvm_symbol = "llvm.nvvm.barrier.cta.sync.aligned.count"
llvm_arguments = ["i32", "i32"]
llvm_results = []
pure = false
memory = "read_write"
convergent = true
execution_scope = "cta"
minimum_ptx = "1.0"
ptx_result = "()"
targets = "all"
ptx_isa_version = "9.3"
ptx_isa_section = "9.7.14.1 Parallel Synchronization and Communication Instructions: bar, barrier"
ptx_isa_url = "https://docs.nvidia.com/cuda/parallel-thread-execution/#parallel-synchronization-and-communication-instructions-bar-barrier"
lowering = "generated_execution_control"
backend_lowerings = [
    { backend = "llvm_nvptx", mechanism = "typed_nvvm", evidence_profile = "rust-llvm-23.1.0-16696adc-execution-control", minimum_ptx = "3.2", minimum_sm = "sm_20" },
    { backend = "lib_nvvm", mechanism = "inline_ptx", evidence_profile = "cuda-13.3-libnvvm-13.3.73-execution-control", minimum_ptx = "1.0", minimum_sm = "sm_75" },
]
expected_ptx = { mnemonic = "bar", modifiers = ["sync"], operands = [{ kind = "register_or_immediate" }, { kind = "register_or_immediate" }] }
summary = "Synchronizes the requested number of CTA threads at an aligned numbered barrier."

[[intrinsic]]
id = "barrier_cta_arrive"
abi_id = "i0885"
operation_key = "synchronization.cta.barrier.arrive.count"
family = "counted_barrier"
source_record = "int_nvvm_barrier_cta_arrive_count"
rust_module = "barrier"
rust_name = "barrier_cta_arrive"
rust_arguments = ["u32", "u32"]
rust_result = "()"
safe = false
must_use = false
public_rust_path = "cuda_intrinsics::barrier::barrier_cta_arrive"
compatibility_rust_paths = ["cuda_device::barrier::barrier_cta_arrive"]
dialect_op_type = "BarrierCtaArriveCountOp"
dialect_op_name = "nvvm.barrier_cta_arrive_count"
dialect_operands = ["i32", "i32"]
dialect_results = []
llvm_symbol = "llvm.nvvm.barrier.cta.arrive.count"
llvm_arguments = ["i32", "i32"]
llvm_results = []
pure = false
memory = "read_write"
convergent = true
execution_scope = "cta"
minimum_ptx = "6.0"
minimum_sm = "sm_30"
ptx_result = "()"
targets = "all"
ptx_isa_version = "9.3"
ptx_isa_section = "9.7.14.1 Parallel Synchronization and Communication Instructions: bar, barrier"
ptx_isa_url = "https://docs.nvidia.com/cuda/parallel-thread-execution/#parallel-synchronization-and-communication-instructions-bar-barrier"
lowering = "generated_execution_control"
backend_lowerings = [
    { backend = "llvm_nvptx", mechanism = "typed_nvvm", evidence_profile = "rust-llvm-23.1.0-16696adc-execution-control", minimum_ptx = "6.0", minimum_sm = "sm_30" },
    { backend = "lib_nvvm", mechanism = "inline_ptx", evidence_profile = "cuda-13.3-libnvvm-13.3.73-execution-control", minimum_ptx = "6.0", minimum_sm = "sm_75" },
]
expected_ptx = { mnemonic = "barrier", modifiers = ["arrive"], operands = [{ kind = "register_or_immediate" }, { kind = "register_or_immediate" }] }
summary = "Signals arrival of the requested number of CTA threads at a numbered barrier without waiting."

[[intrinsic]]
id = "barrier_cta_arrive_aligned"
abi_id = "i0886"
operation_key = "synchronization.cta.bar.arrive.aligned.count"
family = "counted_barrier"
source_record = "int_nvvm_barrier_cta_arrive_aligned_count"
rust_module = "barrier"
rust_name = "barrier_cta_arrive_aligned"
rust_arguments = ["u32", "u32"]
rust_result = "()"
safe = false
must_use = false
public_rust_path = "cuda_intrinsics::barrier::barrier_cta_arrive_aligned"
compatibility_rust_paths = ["cuda_device::barrier::barrier_cta_arrive_aligned"]
dialect_op_type = "BarrierCtaArriveAlignedCountOp"
dialect_op_name = "nvvm.barrier_cta_arrive_aligned_count"
dialect_operands = ["i32", "i32"]
dialect_results = []
llvm_symbol = "llvm.nvvm.barrier.cta.arrive.aligned.count"
llvm_arguments = ["i32", "i32"]
llvm_results = []
pure = false
memory = "read_write"
convergent = true
execution_scope = "cta"
minimum_ptx = "1.0"
ptx_result = "()"
targets = "all"
ptx_isa_version = "9.3"
ptx_isa_section = "9.7.14.1 Parallel Synchronization and Communication Instructions: bar, barrier"
ptx_isa_url = "https://docs.nvidia.com/cuda/parallel-thread-execution/#parallel-synchronization-and-communication-instructions-bar-barrier"
lowering = "generated_execution_control"
backend_lowerings = [
    { backend = "llvm_nvptx", mechanism = "typed_nvvm", evidence_profile = "rust-llvm-23.1.0-16696adc-execution-control", minimum_ptx = "3.2", minimum_sm = "sm_20" },
    { backend = "lib_nvvm", mechanism = "inline_ptx", evidence_profile = "cuda-13.3-libnvvm-13.3.73-execution-control", minimum_ptx = "1.0", minimum_sm = "sm_75" },
]
expected_ptx = { mnemonic = "bar", modifiers = ["arrive"], operands = [{ kind = "register_or_immediate" }, { kind = "register_or_immediate" }] }
summary = "Signals arrival of the requested number of CTA threads at an aligned numbered barrier without waiting."
