# Reviewed f16x2-to-packed-FP8 and packed-FP8-to-f16x2 conversions.
#
# Both directions are single-operand: the source arrives already packed in one
# register rather than as the scalar f32 pair admitted by
# overlay/packed_conversion_fp8.toml.

schema = 59
family = "packed_conversion"

[packed_conversion_fp8_f16x2]
llvm_evidence_profile = "rust-llvm-23.1.0-16696adc"
libnvvm_evidence_profile = "cuda-13.3-libnvvm-13.3.33"
runtime_validation = "unexecuted"
fp8_formats = ["e4m3x2", "e5m2x2"]
directions = ["pack", "unpack"]
relu_variants = true
product_count = 8
