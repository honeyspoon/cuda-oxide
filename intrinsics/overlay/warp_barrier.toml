# Reviewed cuda-oxide policy for masked warp synchronization.

schema = 26
family = "warp_barrier"

[[intrinsic]]
id = "sync_mask"
abi_id = "i0049"
operation_key = "warp.barrier.sync.masked"
family = "warp_barrier"
source_record = "int_nvvm_bar_warp_sync"
rust_module = "warp"
rust_name = "sync_mask"
rust_arguments = ["u32"]
rust_result = "()"
safe = false
must_use = false
public_rust_path = "cuda_intrinsics::warp::sync_mask"
compatibility_rust_paths = ["cuda_device::warp::sync_mask"]
dialect_op_type = "BarWarpSyncOp"
dialect_op_name = "nvvm.bar_warp_sync"
dialect_operands = ["i32"]
dialect_results = []
llvm_symbol = "llvm.nvvm.bar.warp.sync"
llvm_arguments = ["i32"]
llvm_results = []
pure = false
memory = "read_write"
convergent = true
execution_scope = "warp"
minimum_ptx = "6.0"
minimum_sm = "sm_30"
ptx_result = "()"
targets = "all"
ptx_isa_version = "9.3"
ptx_isa_section = "9.7.14.2 Parallel Synchronization and Communication Instructions: bar.warp.sync"
ptx_isa_url = "https://docs.nvidia.com/cuda/parallel-thread-execution/#parallel-synchronization-and-communication-instructions-bar-warp-sync"
lowering = "generated_warp_barrier"
backend_lowerings = [
    { backend = "llvm_nvptx", mechanism = "typed_nvvm", evidence_profile = "rust-llvm-23.1.0-16696adc", minimum_ptx = "6.0", minimum_sm = "sm_30" },
    { backend = "lib_nvvm", mechanism = "typed_nvvm", evidence_profile = "cuda-13.3-libnvvm-13.3.33", minimum_ptx = "6.0", minimum_sm = "sm_75" },
]
warp_barrier = { participation = "executing_lane_named_all_named_lanes_same_instruction_and_mask", legacy_pre_sm70 = "all_named_lanes_converged_and_only_named_lanes_active", adapter = "direct_member_mask", mask_encoding = "register_or_immediate", memory_ordering = "participating_lanes" }
expected_ptx = { mnemonic = "bar", modifiers = ["warp", "sync"], operands = [{ kind = "register_or_immediate" }] }
summary = "Synchronize and order memory for the named warp lanes."
