# Reviewed cuda-oxide policy for CTA synchronization.

schema = 26
family = "sync"

[[intrinsic]]
id = "sync_threads"
abi_id = "i0034"
operation_key = "synchronization.cta.barrier.aligned.all"
family = "sync"
source_record = "int_nvvm_barrier_cta_sync_aligned_all"
rust_module = "thread"
rust_name = "sync_threads"
rust_arguments = []
rust_result = "()"
safe = false
must_use = false
public_rust_path = "cuda_intrinsics::thread::sync_threads"
compatibility_rust_paths = [
    "cuda_device::thread::sync_threads",
    "cuda_device::sync_threads",
]
dialect_op_type = "Barrier0Op"
dialect_op_name = "nvvm.barrier0"
dialect_operands = []
dialect_results = []
llvm_symbol = "llvm.nvvm.barrier.cta.sync.aligned.all"
llvm_arguments = ["i32"]
llvm_results = []
pure = false
memory = "read_write"
convergent = true
execution_scope = "cta"
minimum_ptx = "1.0"
ptx_result = "()"
targets = "all"
ptx_isa_version = "9.3"
ptx_isa_section = "9.7.14.1 Parallel Synchronization and Communication Instructions: bar, barrier"
ptx_isa_url = "https://docs.nvidia.com/cuda/parallel-thread-execution/#parallel-synchronization-and-communication-instructions-bar-barrier"
lowering = "generated_sync_threads"
backend_lowerings = [
    { backend = "llvm_nvptx", mechanism = "typed_nvvm", evidence_profile = "rust-llvm-23.1.0-16696adc", minimum_ptx = "3.2", minimum_sm = "sm_20" },
    { backend = "lib_nvvm", mechanism = "inline_ptx", evidence_profile = "cuda-13.3-libnvvm-13.3.33", minimum_sm = "sm_75" },
]
expected_ptx = { mnemonic = "bar", modifiers = ["sync"], operands = [{ kind = "exact", value = "0" }] }
summary = "Waits until every active thread in the CTA reaches the same barrier, then makes their earlier memory accesses visible to the CTA."
