[package]
name = "shared-memory-helper-lib"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[features]
force-inline = []
heuristic-inline = []

[dependencies]
cuda-device = { path = "../../../../cuda-device" }
