[package]
name = "kernel_reference_validity"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

# Mark as standalone crate (not part of parent workspace).
[workspace]

[dependencies]
cuda-core = "0.3.1"
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host", features = ["async"] }
